spring:
  datasource:
    password: admin
    url: jdbc:h2:mem:db_campibus_payments
    username: admin
  h2:
    console:
      enabled: true
      path: /h2-console
server:
  port: 8081
  servlet:
    context-path: /campibus-payments
